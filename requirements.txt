# ============================================================
# Where Should I Buy? — GeoAI Course Project
# Python >= 3.10 recommended
# ============================================================

# --- Core Geospatial ---
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
fiona>=1.9.0
rtree>=1.0.0          # R-tree spatial index (required by GeoPandas)
pyogrio>=0.7.0        # Fast vector I/O backend for GeoPandas

# --- GeoParquet support ---
pyarrow>=14.0.0       # Needed for GeoParquet read/write

# --- Visualization ---
folium>=0.15.0
streamlit>=1.32.0
streamlit-folium>=0.18.0
matplotlib>=3.8.0
plotly>=5.18.0

# --- Data Science ---
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0

# --- Networking / Data Fetching ---
requests>=2.31.0
osmnx>=1.7.0          # OSM data (transit stops, parks, streets)

# --- Notebook environment ---
jupyterlab>=4.0.0
ipykernel>=6.0.0
ipywidgets>=8.0.0

# --- Utilities ---
python-dotenv>=1.0.0  # .env config loading
tqdm>=4.66.0          # Progress bars
loguru>=0.7.0         # Structured logging
pydantic>=2.5.0       # Data validation models
